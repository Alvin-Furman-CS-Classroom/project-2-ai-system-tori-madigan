Figure 2 (Streamlit UI) — add your own screenshot

The paper uses:
  · Figure 1  — figure1_pipeline.png (generated by scripts/generate_paper_artifacts.py)
  · Table 1   — table1_evaluation_mapping.png
  · Table 2   — table2_module_outcomes.png
  · Figure 2  — your real UI screen capture (not auto-generated)

Suggested filename (optional):
  figure2_streamlit_ui.png

How to capture:
  1. From the repo root:  set PYTHONPATH=src  then  streamlit run streamlit_app.py
  2. Open the local URL, show the grid and clues in one view.
  3. Screenshot the browser window (or use Snipping Tool) and save as PNG.
  4. Insert that image in your final document as Figure 2 next to the caption in final_paper_draft.txt.

You can remove any old auto-generated “figure2” / “figure3” PNGs if they are no longer used.
